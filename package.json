{
  "name": "repo-cartographer",
  "private": true,
  "description": "Monorepo root. The Python agent and API live in backend/; the Next.js app in frontend/.",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "setup": "bash setup.sh",
    "doctor": "cd backend && uv run scripts/check_env.py",
    "dev": "concurrently --names api,web --prefix-colors cyan,magenta \"npm:dev:api\" \"npm:dev:web\"",
    "dev:api": "cd backend && uv run python serve.py --reload",
    "dev:web": "npm run dev --workspace frontend",
    "start:api": "cd backend && uv run python serve.py",
    "build:web": "npm run build --workspace frontend",
    "lint:web": "npm run lint --workspace frontend",
    "lint:api": "cd backend && uv run ruff check . && uv run mypy",
    "test:api": "cd backend && uv run pytest -q",
    "migrate": "cd backend && uv run scripts/apply_migrations.py"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
